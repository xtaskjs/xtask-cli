{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "cache": false
    },
    "test": {
      "dependsOn": ["^build", "build"],
      "cache": false
    },
    "test:coverage": {
      "dependsOn": ["^build", "build"],
      "cache": false
    }
  }
}